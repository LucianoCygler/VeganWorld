const { Product } = require("../../db");

const orderProductByPrice = (precioOrder, orderedProduct) => {
  console.log(orderedProduct);
  console.log(precioOrder);
  return allProducts;
};

module.exports = orderProductByPrice;
